name: DINKFLIX Web
version: 2.2.0.0
jellyfin_version: 12.2.0
jellyfin_abi: 12.2.0.0
framework: net10.0
project: src/Jellyfin.Plugin.DinkFlix/Jellyfin.Plugin.DinkFlix.csproj
publish:
  configuration: Release
  single_file: false
  trim: false

# GitHub website workflow:
# 1. Upload the repository contents through Add file -> Upload files.
# 2. Wait for the validation workflow to turn green.
# 3. Create a GitHub release with a tag such as v2.2.0.0.
# 4. The release workflow builds the plugin ZIP and updates manifest.json.
